# Requirements for Raspberry Pi (Python 3.13 compatible)
# For macOS development, use: pip install -r requirements-mac.txt

# Core dependencies (Python 3.13 requires newer versions with pre-built wheels)
opencv-python~=4.10.0
numpy~=2.0.0
pillow~=11.0.0

# Raspberry Pi camera support (for libcamera systems)
# Note: Install system package: sudo apt install python3-picamera2
# Then create venv with: python3 -m venv --system-site-packages venv
# picamera2  # Must use system package, not pip

# Audio processing
pyaudio~=0.2.14
scipy~=1.14.0
librosa~=0.10.0

# Web framework
flask~=3.0.0
flask-cors~=4.0.0

# Database
sqlalchemy~=2.0.0

# Notifications
requests~=2.32.0
pushbullet.py~=0.12.0

# Configuration
pyyaml~=6.0

# Utilities
python-dotenv~=1.0.0

# TensorFlow for pose estimation (optional - install manually if needed)
# tensorflow~=2.15.0  # May not be available for Python 3.13 yet
